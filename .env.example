# Postgres connection (Vercel / Neon / Supabase)
# Copy to .env and fill in values. Never commit .env.

DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Songstats API (Phase 2 — Beatport chart ingestion)
SONGSTATS_API_KEY=
# SONGSTATS_BASE_URL=https://api.songstats.com

# Vercel Cron: задайте CRON_SECRET (≥16 символів), щоб захистити /api/cron/ingest
CRON_SECRET=
