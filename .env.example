# Postgres connection (Vercel / Neon / Supabase)
# Copy to .env and fill in values. Never commit .env.
# Use sslmode=verify-full to avoid pg SSL warning (or uselibpqcompat=true&sslmode=require for libpq semantics).

DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=verify-full

# Beatport discovery (genre index URL; default: https://www.beatport.com)
# BEATPORT_GENRE_INDEX_URL=https://www.beatport.com

# Songstats API (Phase 2 — Beatport chart ingestion)
SONGSTATS_API_KEY=
# SONGSTATS_BASE_URL=https://api.songstats.com

# BP Top Tracker — ретроспектива по днях (backfill 2–3 міс., потім щоденне оновлення).
# Ніколи не коміть пароль у репо. Якщо пароль потрапив у чат/код — негайно змініть його на bptoptracker.com.
# Варіант 1: логін + пароль (пароль тільки в .env, не в коді)
# BPTOPTRACKER_EMAIL=your@email.com
# BPTOPTRACKER_PASSWORD=your_password
# Варіант 2: вже отриманий session cookie (після логіну в браузері → DevTools → Application → Cookies)
# BPTOPTRACKER_COOKIE=session=...
# Жанри для щоденного оновлення (cron), через кому: afro-house, techno, house
# BPTOPTRACKER_GENRES=afro-house,techno,house

# Vercel Cron: задайте CRON_SECRET (≥16 символів), щоб захистити /api/cron/ingest
CRON_SECRET=

# Phase 6 — Enrichment (optional): LLM для bio/role/insight. Якщо не задано — тільки ручне заповнення.
# OPENAI_API_KEY=
# ENRICHMENT_LLM_API_KEY=
